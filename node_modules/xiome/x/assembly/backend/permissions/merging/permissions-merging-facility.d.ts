import { AnyPermissions } from "../permissions-helpers.js";
import { DamnId } from "../../../../toolbox/damnedb/damn-id.js";
import { RoleHasPrivilegeRow } from "../../../../features/auth/aspects/permissions/types/permissions-tables.js";
export declare function permissionsMergingFacility({ isPlatform }: {
    isPlatform: boolean;
}): {
    hardPermissions: AnyPermissions;
    mergeRoleHasPrivileges: ({ hard, soft }: {
        hard: {
            privilegeId: string;
            roleId: string;
            active: boolean;
            immutable: boolean;
        }[];
        soft: RoleHasPrivilegeRow[];
    }) => {
        roleId: DamnId;
        privilegeId: DamnId;
        immutable: boolean;
        active: boolean;
        time: number;
    }[];
    getActivePrivilegeIds: (roleHasPrivileges: RoleHasPrivilegeRow[]) => DamnId[];
    getHardPrivilegeDetails: (...roleIds: string[]) => {
        privilegeId: string;
        roleId: string;
        active: boolean;
        immutable: boolean;
    }[];
};
