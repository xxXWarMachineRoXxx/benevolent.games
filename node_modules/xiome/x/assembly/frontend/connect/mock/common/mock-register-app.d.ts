import { Await } from "../../../../../types/await.js";
import { backendForNode } from "../../../../backend/backend-for-node.js";
export declare function mockRegisterApp({ ownerEmail, adminEmail, appOrigins, backend }: {
    ownerEmail: string;
    adminEmail: string;
    appOrigins: string[];
    backend: Await<ReturnType<typeof backendForNode>>;
}): Promise<string>;
