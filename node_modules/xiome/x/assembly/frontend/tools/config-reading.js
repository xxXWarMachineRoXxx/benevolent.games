export function configReading(selector) {
    const element = document.querySelector(selector);
    if (!element)
        throw new Error(`${selector} is required`);
    return {
        attr(key) {
            return element.getAttribute(key) ?? undefined;
        },
    };
}
//# sourceMappingURL=config-reading.js.map