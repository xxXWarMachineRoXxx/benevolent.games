export declare class DamnId {
    #private;
    static fromBinary(binary: ArrayBuffer): DamnId;
    static fromString(text: string): DamnId;
    static isId(text: string): boolean;
    constructor(binary: ArrayBuffer);
    get binary(): ArrayBuffer;
    get string(): string;
    toBinary(): ArrayBuffer;
    toString(): string;
}
