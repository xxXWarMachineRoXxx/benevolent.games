import { Observer, Reaction, Readable } from "./types.js";
export declare function trackingMechanics(): {
    readonly wait: Promise<void>;
    track<xState, X>(state: Readable<xState>, observer: Observer<xState, X>, reaction?: Reaction<X>): () => void;
    avoidCircular(key: string): void;
    triggerReactions: <xState_1>(state: Readable<xState_1>, key: string) => void;
    reactionRegistration(key: string): void;
};
