import { trackingMechanics } from "./tracking-mechanics.js";
export declare type Observer<xState, X> = (readable: Readable<xState>) => X;
export declare type Reaction<X> = (x: X) => void;
export declare type Track = ReturnType<typeof trackingMechanics>["track"];
export declare type Readable<xState extends {}> = {
    readonly [P in keyof xState]: xState[P];
};
