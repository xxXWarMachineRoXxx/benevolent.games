import { Observer, Reaction, Readable } from "./parts/types.js";
export declare function snapstate<xState extends {}>(actual: xState): {
    readable: Readable<xState>;
    writable: xState;
    subscribe: (listener: (context: Readable<xState>) => void) => () => void;
    track<X>(observer: Observer<xState, X>, reaction?: Reaction<X>): () => void;
    wait(): Promise<void>;
};
