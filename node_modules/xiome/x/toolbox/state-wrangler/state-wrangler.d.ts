import { Action, Reducers, StateWranglerOptions } from "./types/state-wrangler-types.js";
export declare function stateWrangler<xState>({ initial, render, debug, }: StateWranglerOptions<xState>): <xReducers extends Reducers<xState>>(reducers: xReducers) => { [P in keyof xReducers]: Action<xReducers[P]>; };
