import { Context, ObservableRecord, ScheduledObservable } from "../types/autowatcher-types.js";
export declare function autowatcherCore(): {
    context: Context;
    subscribe: (subscription: ScheduledObservable) => {
        subscription: ScheduledObservable;
        record: ObservableRecord;
    };
    triggerObservation: (object: {}, key: string) => void;
};
