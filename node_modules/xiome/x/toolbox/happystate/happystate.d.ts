import { AnyListener, Subscribe } from "../pubsub.js";
export declare type Actions = {
    [key: string]: (...args: any[]) => void;
};
export interface Happy<xState extends {}, xActions extends Actions> {
    actions: xActions;
    getState: () => xState;
    clearStateListeners: () => void;
    onStateChange: Subscribe<AnyListener>;
}
export declare function happystate<xState extends {}, xActions extends Actions>({ state: realState, actions: makeActions, }: {
    state: xState;
    actions: (state: xState) => xActions;
}): Happy<xState, xActions>;
