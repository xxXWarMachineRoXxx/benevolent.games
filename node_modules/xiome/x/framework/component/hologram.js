import { snapstate } from "../../toolbox/snapstate/snapstate.js";
export function property(type) {
    return type;
}
export function hologramComponent({}) {
    const state = snapstate({});
    const Component = class extends HTMLElement {
    };
    function virtual(state) {
    }
    virtual.Component = Component;
    return virtual;
}
//
//
// playing around
//
//
const test = hologramComponent({
    required: {
        haha: property(String),
        lmao: property(Boolean),
    },
    optional: {
        rofl: property(Object),
    },
    init({ readable, writable }) {
    },
    render: state => `
		${state.haha}
	`,
});
test({
    haha: ":)",
    lmao: true,
    rofl: { x: 1 },
});
//# sourceMappingURL=hologram.js.map