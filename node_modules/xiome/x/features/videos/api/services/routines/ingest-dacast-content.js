export function ingestDacastContent({ type, data, }) {
    const pictures = (data.pictures ?? []);
    const picture = pictures[0];
    const thumb = picture
        ? (picture.thumbnail ?? [])[0]
        : undefined;
    return {
        type,
        thumb,
        id: data.id,
        title: data.title,
        provider: "dacast",
    };
}
//# sourceMappingURL=ingest-dacast-content.js.map