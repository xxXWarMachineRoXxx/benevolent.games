import * as renraku from "renraku";
import { VerifyToken } from "redcrypto/x/types.js";
import { AuthTables } from "../types/auth-tables.js";
import { AppTables } from "../aspects/apps/types/app-tables.js";
import { SecretConfig } from "../../../assembly/backend/types/secret-config.js";
import { UnconstrainedTables } from "../../../framework/api/types/table-namespacing-for-apps.js";
import { AnonMeta, AppOwnerAuth, AppOwnerMeta, GreenAuth, LoginAuth, PlatformUserAuth, PlatformUserMeta, UserAuth, UserMeta } from "../types/auth-metas.js";
export declare function prepareAuthPolicies({ config, appTables, authTables, verifyToken, }: {
    config: SecretConfig;
    appTables: AppTables;
    authTables: UnconstrainedTables<AuthTables>;
    verifyToken: VerifyToken;
}): {
    greenPolicy: renraku.Policy<undefined, GreenAuth>;
    anonPolicy: renraku.Policy<AnonMeta, LoginAuth>;
    userPolicy: renraku.Policy<UserMeta, UserAuth>;
    platformUserPolicy: renraku.Policy<PlatformUserMeta, PlatformUserAuth>;
    appOwnerPolicy: renraku.Policy<AppOwnerMeta, AppOwnerAuth>;
};
