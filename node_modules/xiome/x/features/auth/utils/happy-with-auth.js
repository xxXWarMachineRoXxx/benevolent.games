import { ops } from "../../../framework/ops.js";
import { happyCombine } from "../../../toolbox/happystate/happy-combine.js";
import { happystate } from "../../../toolbox/happystate/happystate.js";
export function happyWithAuth(base) {
    const happyAuth = happystate({
        state: {
            accessOp: ops.none()
        },
        actions: state => ({
            setAccessOp(op) {
                state.accessOp = op;
            },
        }),
    });
    const happy = happyCombine(base)(happyAuth).combine();
    return {
        happy,
        updateAccessOp(op) {
            happy.actions.setAccessOp(op);
        },
    };
    return;
}
//# sourceMappingURL=happy-with-auth.js.map