import { SignToken } from "redcrypto/x/types.js";
import { AuthTables } from "../../../../types/auth-tables.js";
import { DamnId } from "../../../../../../toolbox/damnedb/damn-id.js";
import { Scope } from "../../../../types/auth-tokens.js";
import { PermissionsEngine } from "../../../../../../assembly/backend/permissions/types/permissions-engine.js";
export declare function signAuthTokens({ scope, appId, userId, origins, lifespans, authTables, permissionsEngine, signToken, }: {
    scope: Scope;
    appId: string;
    userId: DamnId;
    origins: string[];
    authTables: AuthTables;
    permissionsEngine: PermissionsEngine;
    lifespans: {
        access: number;
        refresh: number;
    };
    signToken: SignToken;
}): Promise<import("../../../../../../toolbox/concurrent.js").AwaitProps<{
    accessToken: Promise<string>;
    refreshToken: Promise<string>;
}>>;
