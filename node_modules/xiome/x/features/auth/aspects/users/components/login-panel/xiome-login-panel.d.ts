import { makeAccessModel } from "../../models/access-model.js";
import { ComponentWithShare } from "../../../../../../framework/component.js";
export declare class XiomeLoginPanel extends ComponentWithShare<{
    accessModel: ReturnType<typeof makeAccessModel>;
}> {
    ["show-logout"]: boolean;
    ["status"]: "loading" | "logged-out" | "logged-in";
    private sentLoading;
    private textInput;
    private emailIsValid;
    private sendEmail;
    private resetSentLoading;
    private logout;
    subscribe(): () => void;
    private renderLoggedIn;
    private handleEmailChange;
    private renderLegalLink;
    private renderLoggedOut;
    render(): import("lit-html").TemplateResult<1>;
}
