import { Await } from "../../../types/await.js";
import { DamnId } from "../../../toolbox/damnedb/damn-id.js";
import { DbbyTable } from "../../../toolbox/dbby/dbby-types.js";
import { mockStoreTables } from "../api/tables/mock-store-tables.js";
export declare type StoreTables = Await<ReturnType<typeof mockStoreTables>>;
export declare type MerchantTables = {
    stripeAccounts: DbbyTable<MerchantRow>;
};
export declare type MerchantRow = {
    time: number;
    userId: DamnId;
    paused: boolean;
    stripeAccountId: string;
};
export declare type SubscriptionTables = {
    plans: DbbyTable<SubscriptionPlanRow>;
    tiers: DbbyTable<SubscriptionTierRow>;
};
export declare type SubscriptionPlanRow = {
    label: string;
    planId: DamnId;
    roleId: DamnId;
    time: number;
    stripeAccountId: string;
    stripeProductId: string;
};
export declare type SubscriptionTierRow = {
    label: string;
    tierId: DamnId;
    planId: DamnId;
    roleId: DamnId;
    time: number;
    stripePriceId: string;
    stripeAccountId: string;
};
