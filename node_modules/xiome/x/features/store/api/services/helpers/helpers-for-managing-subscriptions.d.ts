import { DamnId } from "../../../../../toolbox/damnedb/damn-id.js";
import { StoreLinkedAuth } from "../../../types/store-metas-and-auths.js";
export declare const helpersForManagingSubscriptions: ({ authTables, storeTables, stripeAccountId, stripeLiaisonAccount, generateId, }: StoreLinkedAuth & {
    generateId: () => DamnId;
}) => {
    createStripeProductAndPrice(options: {
        planLabel: string;
        tierPrice: number;
        tierCurrency: string;
        tierInterval: "month" | "year";
    }): Promise<{
        stripeProductId: string;
        stripePriceId: string;
    }>;
    createPlanAndTier({ planLabel, tierLabel, stripePriceId, stripeProductId, }: {
        planLabel: string;
        tierLabel: string;
        stripePriceId: string;
        stripeProductId: string;
    }): Promise<{
        planId: DamnId;
        tierId: DamnId;
        time: number;
    }>;
};
