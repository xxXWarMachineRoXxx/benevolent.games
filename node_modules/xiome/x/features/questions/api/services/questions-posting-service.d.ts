import * as renraku from "renraku";
import { QuestionDraft } from "../types/question-draft.js";
import { UserMeta } from "../../../auth/types/auth-metas.js";
import { Question } from "../types/questions-and-answers.js";
import { DamnId } from "../../../../toolbox/damnedb/damn-id.js";
import { QuestionsApiOptions } from "../types/questions-api-options.js";
export declare const makeQuestionsPostingService: (options: QuestionsApiOptions) => renraku.Service<UserMeta, import("../../../auth/types/auth-metas.js").UserAuth & {
    questionsTables: import("../../../../toolbox/dbby/dbby-types.js").DbbyConstrainTables<{
        "namespace-appId": DamnId;
    }, import("../tables/types/questions-tables.js").QuestionsTables>;
}, {
    postQuestion(inputs: {
        questionDraft: QuestionDraft;
    }): Promise<Question>;
    archiveQuestion(inputs: {
        archive: boolean;
        questionId: string;
    }): Promise<void>;
    likeQuestion(inputs: {
        like: boolean;
        questionId: string;
    }): Promise<void>;
    reportQuestion(inputs: {
        report: boolean;
        questionId: string;
    }): Promise<void>;
}>;
