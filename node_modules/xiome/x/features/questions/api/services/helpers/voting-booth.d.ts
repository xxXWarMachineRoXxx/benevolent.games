import { DamnId } from "../../../../../toolbox/damnedb/damn-id.js";
import { SimpleVoteTable } from "../../tables/types/questions-tables.js";
export declare function makeVotingBooth({ userId, itemIds, likesTable, reportsTable }: {
    userId?: DamnId;
    itemIds: DamnId[];
    likesTable: SimpleVoteTable;
    reportsTable: SimpleVoteTable;
}): Promise<{
    getVotingDetails(itemId: DamnId): {
        likes: number;
        reports: number;
        liked: boolean;
        reported: boolean;
    };
}>;
