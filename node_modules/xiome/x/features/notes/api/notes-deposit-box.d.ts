import { Rando } from "../../../toolbox/get-rando.js";
import { NotesTables } from "./tables/notes-tables.js";
import { DraftForNote, Notes } from "../types/notes-concepts.js";
export declare function makeNotesDepositBox({ rando, notesTables }: {
    rando: Rando;
    notesTables: NotesTables;
}): {
    sendNotes: (drafts: DraftForNote<Notes.Any>[]) => Promise<{
        noteId: string;
    }[]>;
    sendNote: (draft: DraftForNote<Notes.Any>) => Promise<{
        noteId: string;
    }>;
};
