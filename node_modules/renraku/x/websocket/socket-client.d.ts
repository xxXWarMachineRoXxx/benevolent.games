import { Api, MetaMap } from "../types.js";
export declare function webSocketClient<xServerApi extends Api>({ link, metaMap, clientApi, handleConnectionClosed, }: {
    link: string;
    metaMap: MetaMap<xServerApi>;
    clientApi: Api;
    handleConnectionClosed(): void;
}): Promise<{
    remote: import("../types.js").ApiRemote<xServerApi>;
    close: () => void;
}>;
